public class VariableAssingment {
    public void VariableAssingmentMethod() {
        string batchName = 'Batch16';
        Integer noOfStudents = 50;
        String description = 'This is the best batch and we are doing great';
        
        System.debug(batchName);
        System.debug(noOfStudents);
        System.debug(description);

    }
}